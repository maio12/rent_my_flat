
<%= simple_form_for [@reservation,@review], url: reservation_reviews_path, action: :post do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: Review::RATINGS %>
  <%= f.submit "add a review", class: "btn-review btn-primary" %>
<% end %>
